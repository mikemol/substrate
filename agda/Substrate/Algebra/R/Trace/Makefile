# GENERATED by scripts/gen_build_makefiles.py — do not edit by hand.
# Per-file, memory-confined Agda check for this directory.
# make = dispatcher only: it ALWAYS invokes agda per file; agda
# decides cached-vs-recheck (transitively correct). FILES and
# SUBDIRS are listed explicitly — a tree/makefile mismatch fails
# loudly (re-run scripts/gen_build_makefiles.py).

# membudget agda-shim → make is the controlled+instrumented build path (RAM-budgeted + proof-cost
# profiled). Idempotent (findstring); exported to recipes + sub-makes. MEMBUDGET_NOPROFILE=1 to skip
# profiling; `make AGDA=…` to pin a different agda.
SHIM_DIR := /home/mikemol/github/substrate/scripts/agda-shim
ifeq (,$(findstring $(SHIM_DIR),$(PATH)))
export PATH := $(SHIM_DIR):$(PATH)
endif

AGDA ?= agda
AGDA_FLAGS ?= --safe --without-K
MEM_CAP ?= 1024m
AGDA_ROOT := ../../../..
SCRIPTS := ../../../../../scripts

FILES := Bisim.agda CFBezoutBridge.agda CFBezoutCross.agda CFBezoutPrev.agda CFDigitHet.agda CFMatrixBridge.agda Cancel.agda ChiralityBridge.agda CycleWire.agda DetSign.agda DetValSign.agda EventualPeriod.agda Exp.agda ExtruderBraidRealigned.agda ExtruderFix.agda ExtruderLambekRealigned.agda ExtruderMiscRealigned.agda ExtruderNfCrossDlog.agda ExtruderNfCrossDlogCospan.agda ExtruderNfCrossRealigned.agda ExtruderNfGradedRealigned.agda ExtruderObsCoalg.agda ExtruderQSideRealigned.agda ExtruderRSideRealigned.agda Final.agda Log.agda NewmanKI.agda NewmanSKI.agda Ops.agda OrbitAudit.agda OrbitBoundary.agda OrbitComplete.agda OrbitFaithful.agda OrbitUniversality.agda PeriodicCover.agda Properties.agda RationalAdjunction.agda SKIAllegoryFixpoints.agda SKIFaithfulOnGraded.agda SKIFaithfulRb.agda SKIGradedFlatAllegory.agda SKIGradedTraceChain.agda SKIOnCombinatorDivStr.agda SKIReduceResidueFP.agda SKIReductionToList.agda SKIShedDuality.agda SKITermToShedding.agda SeriesCancel.agda StreamMap.agda Transcendental.agda Unfold.agda V4FullCocycle.agda WedgeBridge.agda Window.agda Windowed.agda
SUBDIRS := CFBezoutBridge CFMatrixBridge NewmanKI NewmanSKI

.PHONY: all files subdirs $(SUBDIRS) ingest ingest-files ingest-subdirs $(SUBDIRS:%=%.ingest) check-sync clean

all: check-sync subdirs files

# Blow up if the directory's actual contents differ from this
# generated listing (new/removed file or subdir → regenerate).
check-sync:
	@disk_files=`ls *.agda 2>/dev/null | sort`; \
	 listed=`printf '%s\n' $(FILES) | sed '/^$$/d' | sort`; \
	 if [ "$$disk_files" != "$$listed" ]; then \
	   echo 'ERROR: $(CURDIR): .agda files differ from generated FILES list.'; \
	   echo '  on disk : '"`echo $$disk_files | tr '\n' ' '`"; \
	   echo '  listed  : '"`echo $$listed | tr '\n' ' '`"; \
	   echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	 fi

# One agda process PER FILE, RTS heap-capped, invoked from agda/
# so the .agda-lib / _build resolve. Always runs; agda no-ops if
# the interface is already current.
files:
	@for f in $(FILES); do \
	  echo "  check $(CURDIR)/$$f"; \
	  $(AGDA) $(AGDA_FLAGS) +RTS -M$(MEM_CAP) -RTS -i $(AGDA_ROOT) $(CURDIR)/$$f || exit 1; \
	done

subdirs: $(SUBDIRS)

CFBezoutBridge:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

CFMatrixBridge:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

NewmanKI:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

NewmanSKI:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

ingest: ingest-files ingest-subdirs
ingest-files:
	@for f in $(FILES); do \
	  python3 $(SCRIPTS)/sppf_db.py --ingest-core $(CURDIR)/$$f || exit 1; \
	done
ingest-subdirs: CFBezoutBridge.ingest CFMatrixBridge.ingest NewmanKI.ingest NewmanSKI.ingest
CFBezoutBridge.ingest:
	@$(MAKE) -C CFBezoutBridge ingest
CFMatrixBridge.ingest:
	@$(MAKE) -C CFMatrixBridge ingest
NewmanKI.ingest:
	@$(MAKE) -C NewmanKI ingest
NewmanSKI.ingest:
	@$(MAKE) -C NewmanSKI ingest

clean:
	@$(MAKE) -C CFBezoutBridge clean 2>/dev/null || true
	@$(MAKE) -C CFMatrixBridge clean 2>/dev/null || true
	@$(MAKE) -C NewmanKI clean 2>/dev/null || true
	@$(MAKE) -C NewmanSKI clean 2>/dev/null || true
	@true
