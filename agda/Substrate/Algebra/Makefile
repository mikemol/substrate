# GENERATED by scripts/gen_build_makefiles.py — do not edit by hand.
# Per-file, memory-confined Agda check for this directory.
# make = dispatcher only: it ALWAYS invokes agda per file; agda
# decides cached-vs-recheck (transitively correct). FILES and
# SUBDIRS are listed explicitly — a tree/makefile mismatch fails
# loudly (re-run scripts/gen_build_makefiles.py).

# membudget agda-shim → make is the controlled+instrumented build path (RAM-budgeted + proof-cost
# profiled). Idempotent (findstring); exported to recipes + sub-makes. MEMBUDGET_NOPROFILE=1 to skip
# profiling; `make AGDA=…` to pin a different agda.
SHIM_DIR := /home/mikemol/github/substrate/scripts/agda-shim
ifeq (,$(findstring $(SHIM_DIR),$(PATH)))
export PATH := $(SHIM_DIR):$(PATH)
endif

AGDA ?= agda
AGDA_FLAGS ?= --safe --without-K
MEM_CAP ?= 1024m
AGDA_ROOT := ../..
SCRIPTS := ../../../scripts

FILES := AbelianGroup.agda Bijection.agda CayleyDickson.agda CommutativeRing.agda ExpLogCodec.agda F2-CommutativeMonoid.agda F2.agda Field.agda GL3F2.agda Group.agda GroupAction.agda Involution.agda IsomorphicCocycle.agda Magma.agda Medial.agda Module.agda Monoid.agda N-to-F2-Parity.agda PointedSet.agda PontryaginDual.agda PrimeFactor-168-FieldFanOut.agda Q.agda Quotient.agda Ring.agda SemidirectProduct.agda Semigroup.agda Semiring.agda Setoid.agda SetoidGroup.agda TopologicalGroup.agda Torsor.agda Wedge.agda Z.agda Z6-FieldBond.agda Z6-FieldTower.agda
SUBDIRS := Abelian CayleyDickson F1 F2 Fin GL3F2 Group Lie Linear List Module Monoid N-to-F2-Parity Nat ParityKernel PointedSet Polynomial PontryaginDual Q Quotient R Semiring Setoid SetoidGroup Sporadic TopologicalGroup V-Arc Wedge Z

.PHONY: all files subdirs $(SUBDIRS) ingest ingest-files ingest-subdirs $(SUBDIRS:%=%.ingest) check-sync clean

all: check-sync subdirs files

# Blow up if the directory's actual contents differ from this
# generated listing (new/removed file or subdir → regenerate).
check-sync:
	@disk_files=`ls *.agda 2>/dev/null | sort`; \
	 listed=`printf '%s\n' $(FILES) | sed '/^$$/d' | sort`; \
	 if [ "$$disk_files" != "$$listed" ]; then \
	   echo 'ERROR: $(CURDIR): .agda files differ from generated FILES list.'; \
	   echo '  on disk : '"`echo $$disk_files | tr '\n' ' '`"; \
	   echo '  listed  : '"`echo $$listed | tr '\n' ' '`"; \
	   echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	 fi

# One agda process PER FILE, RTS heap-capped, invoked from agda/
# so the .agda-lib / _build resolve. Always runs; agda no-ops if
# the interface is already current.
files:
	@for f in $(FILES); do \
	  echo "  check $(CURDIR)/$$f"; \
	  $(AGDA) $(AGDA_FLAGS) +RTS -M$(MEM_CAP) -RTS -i $(AGDA_ROOT) $(CURDIR)/$$f || exit 1; \
	done

subdirs: $(SUBDIRS)

Abelian:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

CayleyDickson:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

F1:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

F2:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Fin:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

GL3F2:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Group:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Lie:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Linear:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

List:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Module:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Monoid:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

N-to-F2-Parity:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Nat:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

ParityKernel:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

PointedSet:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Polynomial:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

PontryaginDual:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Q:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Quotient:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

R:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Semiring:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Setoid:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

SetoidGroup:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Sporadic:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

TopologicalGroup:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

V-Arc:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Wedge:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Z:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

ingest: ingest-files ingest-subdirs
ingest-files:
	@for f in $(FILES); do \
	  python3 $(SCRIPTS)/sppf_db.py --ingest-core $(CURDIR)/$$f || exit 1; \
	done
ingest-subdirs: Abelian.ingest CayleyDickson.ingest F1.ingest F2.ingest Fin.ingest GL3F2.ingest Group.ingest Lie.ingest Linear.ingest List.ingest Module.ingest Monoid.ingest N-to-F2-Parity.ingest Nat.ingest ParityKernel.ingest PointedSet.ingest Polynomial.ingest PontryaginDual.ingest Q.ingest Quotient.ingest R.ingest Semiring.ingest Setoid.ingest SetoidGroup.ingest Sporadic.ingest TopologicalGroup.ingest V-Arc.ingest Wedge.ingest Z.ingest
Abelian.ingest:
	@$(MAKE) -C Abelian ingest
CayleyDickson.ingest:
	@$(MAKE) -C CayleyDickson ingest
F1.ingest:
	@$(MAKE) -C F1 ingest
F2.ingest:
	@$(MAKE) -C F2 ingest
Fin.ingest:
	@$(MAKE) -C Fin ingest
GL3F2.ingest:
	@$(MAKE) -C GL3F2 ingest
Group.ingest:
	@$(MAKE) -C Group ingest
Lie.ingest:
	@$(MAKE) -C Lie ingest
Linear.ingest:
	@$(MAKE) -C Linear ingest
List.ingest:
	@$(MAKE) -C List ingest
Module.ingest:
	@$(MAKE) -C Module ingest
Monoid.ingest:
	@$(MAKE) -C Monoid ingest
N-to-F2-Parity.ingest:
	@$(MAKE) -C N-to-F2-Parity ingest
Nat.ingest:
	@$(MAKE) -C Nat ingest
ParityKernel.ingest:
	@$(MAKE) -C ParityKernel ingest
PointedSet.ingest:
	@$(MAKE) -C PointedSet ingest
Polynomial.ingest:
	@$(MAKE) -C Polynomial ingest
PontryaginDual.ingest:
	@$(MAKE) -C PontryaginDual ingest
Q.ingest:
	@$(MAKE) -C Q ingest
Quotient.ingest:
	@$(MAKE) -C Quotient ingest
R.ingest:
	@$(MAKE) -C R ingest
Semiring.ingest:
	@$(MAKE) -C Semiring ingest
Setoid.ingest:
	@$(MAKE) -C Setoid ingest
SetoidGroup.ingest:
	@$(MAKE) -C SetoidGroup ingest
Sporadic.ingest:
	@$(MAKE) -C Sporadic ingest
TopologicalGroup.ingest:
	@$(MAKE) -C TopologicalGroup ingest
V-Arc.ingest:
	@$(MAKE) -C V-Arc ingest
Wedge.ingest:
	@$(MAKE) -C Wedge ingest
Z.ingest:
	@$(MAKE) -C Z ingest

clean:
	@$(MAKE) -C Abelian clean 2>/dev/null || true
	@$(MAKE) -C CayleyDickson clean 2>/dev/null || true
	@$(MAKE) -C F1 clean 2>/dev/null || true
	@$(MAKE) -C F2 clean 2>/dev/null || true
	@$(MAKE) -C Fin clean 2>/dev/null || true
	@$(MAKE) -C GL3F2 clean 2>/dev/null || true
	@$(MAKE) -C Group clean 2>/dev/null || true
	@$(MAKE) -C Lie clean 2>/dev/null || true
	@$(MAKE) -C Linear clean 2>/dev/null || true
	@$(MAKE) -C List clean 2>/dev/null || true
	@$(MAKE) -C Module clean 2>/dev/null || true
	@$(MAKE) -C Monoid clean 2>/dev/null || true
	@$(MAKE) -C N-to-F2-Parity clean 2>/dev/null || true
	@$(MAKE) -C Nat clean 2>/dev/null || true
	@$(MAKE) -C ParityKernel clean 2>/dev/null || true
	@$(MAKE) -C PointedSet clean 2>/dev/null || true
	@$(MAKE) -C Polynomial clean 2>/dev/null || true
	@$(MAKE) -C PontryaginDual clean 2>/dev/null || true
	@$(MAKE) -C Q clean 2>/dev/null || true
	@$(MAKE) -C Quotient clean 2>/dev/null || true
	@$(MAKE) -C R clean 2>/dev/null || true
	@$(MAKE) -C Semiring clean 2>/dev/null || true
	@$(MAKE) -C Setoid clean 2>/dev/null || true
	@$(MAKE) -C SetoidGroup clean 2>/dev/null || true
	@$(MAKE) -C Sporadic clean 2>/dev/null || true
	@$(MAKE) -C TopologicalGroup clean 2>/dev/null || true
	@$(MAKE) -C V-Arc clean 2>/dev/null || true
	@$(MAKE) -C Wedge clean 2>/dev/null || true
	@$(MAKE) -C Z clean 2>/dev/null || true
	@true
