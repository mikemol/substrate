# DBE: M-11.fano — chirality at every Fano point + Hamming syndrome as phase

**Cross-reference:** parent plan at `agda/Substrate/Cocycles.DBE.md`;
prior slices M-9 (Fano), M-11.dim3 (`HodgeComplement-Dim3` for the
specific p=p₄ chirality choice), M-7 (Hamming [7,4,3]). Memories:
`project_3plus1_parity_universal`, `feedback_universal_property_discipline`.

This is the **smallest concrete step** in the larger conjecture
(catalog § chirality-rotation framework): chirality choices form a
phase space (PG(2, F₂) = Fano plane = 7 points at n=3) acted on by
GL(3, F₂); the Hamming syndrome IS the literal `vector ↦ chirality
phase` map; transitions between phases compose through codes.

## Step 1 — Halt and name the target

**In scope:**

- Parametric chirality construction at any Fano point: chirality-projection,
  V₄-plane-at-p (KernelCode), chirality-axis-at-p (predicate form).
- 7 named instances (one per Fano point) consuming the parametric form.
- Hamming syndrome identification: `apply H (basis i) ≡ point-coords i`,
  i.e., the syndrome of a single-bit error at position i IS the i-th Fano
  point's coordinates. The Hamming H : Vector 7 → Vector 3 is literally
  a `vector ↦ chirality-phase` map.
- One new foundational primitive: `apply-linear-from-images-basis`
  (universal-property discipline; replaces 7 case-specific refl chains
  with one parametric lemma).

**Out of scope (deferred):**

- Full GL(3, F₂) transitivity proof on Fano points (deferred to
  M-11.fano.gl3; the group has order 168).
- KernelCode form for chirality-axis at general p (would require choosing
  basis for v's complement per p; the predicate `Σ F₂ (λ c → u ≡ c *ₛ v)`
  suffices).
- Full orthogonality proof parametric in p (the existing M-11.dim3
  covers p=p₄; analogous proofs for other p follow the same pattern via
  dot-product bilinearity).
- Λ²(F₂⁴) / higher-grade Hodge ★ (medium step, separate slice).

## Step 2 — Search for repeatable form

Every Fano point p induces:

- A chirality-projection `Linear 3 → 1` defined as "dot product with `point-coords p`."
  Built via `linear-from-images` with basis-images `λ i → lookup (point-coords p) i ∷ []`.
- A V₄-plane = kernel = vectors orthogonal to `point-coords p`.
- A chirality-axis = `⟨point-coords p⟩` = the 1-dim subspace generated.

The repeatable form is **Fano point → chirality structure**, parametric in p : Point.

Per orbit-saturation discipline: don't extract a `ChiralityAtPoint` wrapper
record. The parametric construction IS the orbit-presentation; the 7 named
instances are just specific points of the orbit.

## Step 3 — Name the costructure (shadows)

**N-1 — `apply-linear-from-images-basis`**: foundational primitive
  added to `Substrate/Algebra/F2/Linear/FromImages.agda`. Statement:
  ```
  apply (linear-from-images f) (basis i) ≡ f i
  ```
  Universal-property discipline (per memory). Used directly for the
  syndrome identification; reused for all future "apply on basis"
  proofs across the M-11 tower and beyond.

**N-2 — Parametric chirality-at-p**: `chirality-projection : Point → Linear 3 1`
  + `V4Plane-At : Point → KernelCode 3 1` + `ChiralityAxis-At-Pred : Point → Vector 3 → Set`.
  Uses `apply-linear-from-images-lookup` discipline for the apply reduction.

**N-3 — Syndrome identification**: prove
  `apply (parity-check Hamming-7-4-3) (basis i) ≡ point-coords i`.
  Via N-1 applied to `H-cols` (since point-coords = H-cols by M-9's definition).
  This is the **literal identification** of the Hamming syndrome map
  with the "vector ↦ Fano chirality phase" map.

**N-4 — Seven named instances**: `hodge-at-p₁ … hodge-at-p₇`. Just
  parametric construction applied at each `pᵢ`. Documents that 7 Fano
  points = 7 distinct chirality phases, consistent with `M-11.dim3`'s
  `HodgeComplement-Dim3` (which is the p=p₄ instance with chirality-axis
  = `⟨e₃⟩`).

**N-5 — Capstone connection** (in docstring): note that M-11.dim3 is
  the specific instance, M-11.fano is the parametric family, and the
  syndrome map is the F₂⁷ → F₂³ machine that "reads" which phase a vector
  is closest to.

## Step 4 — Composition

```text
M-7 H-cols ──── M-9 point-coords (= H-cols) ──┐
                                              │
                                              ├─── N-2 (parametric chirality-at-p)
M-5 linear-from-images ──── N-1 apply-basis ──┤        │
                                              │        │
                                              │        ├─── N-3 (syndrome ID)
                                              │        │
                                              └────────┴─── N-4 (7 named instances)
                                                              │
                                                              └─── N-5 (capstone docstring)
```

Composition operation: **parametric construction over Point + the
universal-property apply-basis primitive**.

## Step 5 — Entailment

**Entailment claim:** the parametric construction at any Fano point p
gives the same shape of Hodge-complement structure as M-11.dim3's
specific p=p₄ case. The 7 instances are gauge-equivalent under GL(3, F₂)
(transitivity proof deferred). The Hamming syndrome is the canonical
"vector ↦ which chirality phase" map at the F₂⁷ → F₂³ scale.

**BHK content:** the parametric construction IS the proof transformer:
given a Fano point, produce the chirality structure at that point. The
syndrome identification IS the proof that Hamming's parity-check matrix
ALREADY encodes Fano-point lookups.

## Step 6 — Multi-angle reserve

If N-1's `apply-linear-from-images-basis` proof is structurally awkward
(needs basis-sum-collapse helper), fall back to:

- **Angle B — 7-case syndrome identification**: prove syndrome each
  basis case by direct case analysis (refl-or-near-refl per i). ~40
  lines vs ~30 for the generic. Less reusable but lands the syndrome
  identification with less foundational work.

If the parametric chirality-projection apply-reduction is complex,
fall back to:

- **Angle B — 7 case-specific Selectors** (one per Fano point), each
  built via `linear-from-images` like M-11.dim3's V4Plane-Selector. Loses
  the parametric framing but each case is small.

## Step 7 — Work in terms of named structure

Implementation order:

1. **N-1** first (foundational primitive) — addition to FromImages.agda.
   Verify it typechecks.
2. **N-2** parametric construction in `Geometry/HodgeDim3/Fano.agda`.
3. **N-3** syndrome identification (uses N-1).
4. **N-4** + **N-5** in the same file.
5. *Stop and commit* before scoping the medium step (Λ²(F₂⁴)).

## Effort estimate

| Shadow | Lines (est.) | Risk |
|--------|-------------|------|
| N-1 apply-linear-from-images-basis | ~30 | low-medium (basis-sum-collapse helper) |
| N-2 parametric chirality | ~100 | low |
| N-3 syndrome ID | ~30 | low (refl-or-N-1) |
| N-4 + N-5 | ~50 | low |
| **Total** | **~210** | **low** |

(Originally estimated ~150; revised up to ~210 with the parametric
construction's apply-reduction proof + universal-property discipline
application.)

## Success criteria

1. All files compile under `--safe --without-K`.
2. `apply-linear-from-images-basis` is provable + reusable.
3. Parametric chirality construction matches M-11.dim3's specific
   instance at p=p₄ (consistency check).
4. Syndrome identification closes with no holes; the map `vector ↦
   Fano phase` is named explicitly.

## Lattice classification

- **Planning fire**: region #2 (DBE alone, scoping memo).
- **Implementation slice**: expected region #8 (DRS-triple) — D for the
  parametric construction design; R for the apply-linear-from-images-basis
  extraction (≥3 uses anticipated: syndrome ID + N-2's apply reduction
  + future M-11 lifts); S for cataloguing the 7 instances + syndrome
  identification.

## Connection to the larger conjecture (rotation phases via codes)

M-11.fano grounds the smallest layer of your conjecture: the chirality-
choice phase space at n=3 IS the Fano plane (7 points), and the Hamming
syndrome IS the map that assigns phases. The medium step (M-11.dim4 or
Λ²-Hodge) and full machinery (transfer maps, GL(n, F₂) transitivity)
build on top.

The room for higher category theory you noted: each chirality choice is
an object; transitions via codes are morphisms; the syndrome map is the
functor from "vectors in F₂^n" to "phases" — this is a 1-category at
n=3, but the Λ²-machinery at n=4 will reveal natural 2-cells, and the
transfer maps will give 2-functors. The substrate is extensible.
