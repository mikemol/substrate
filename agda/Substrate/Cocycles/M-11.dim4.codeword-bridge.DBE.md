# DBE: M-11.dim4.codeword-bridge — Codeword.Reserved ↔ SelfDual formal bridge

**Cross-reference:** parent plan at `agda/Substrate/Cocycles.DBE.md`;
M-11.dim4 (Λ²(F₂⁴) + Hodge ★ + SelfDual); M-11.dim4.reserved-bridge
(SelfDual ↔ Vector 3); Codeword.agda (Reserved : Σ Codeword IsReserved).

**The gap.** M-11.dim4.reserved-bridge validated the catalog conjecture
("8 reserved = signed singletons") at the F₂³-proxy level (SelfDual ↔
Vector 3). But it doesn't formally connect to Codeword.agda's actual
Reserved type. This slice closes that gap, completing the conjecture
validation end-to-end:

  **Codeword.Reserved ↔ Vector 3 ↔ SelfDual**

After this slice, the 8 reserved codewords are FORMALLY identified
with the 8 self-dual bivectors of Hodge ★ in Λ²(F₂⁴).

## Step 1 — Halt and name the target

**In scope:**

- `Bool ↔ F₂` direct bijection (foundational primitive; minimal subset
  of M-10A N-1, just the bare bijection without the full F₂-Like
  universal-property packaging).
- `reserved-to-vector3 : Reserved → Vector 3` and inverse
  `vector3-to-reserved : Vector 3 → Reserved`, extracting/inserting
  the 3 free bits (bit₀, bit₁, bit₂) of a Reserved codeword.
- Both round-trips, validating the bijection.
- Composition with `vector3-to-selfdual` from M-11.dim4.reserved-bridge
  to derive `reserved-to-selfdual : Reserved → SelfDual-Bivector` and
  inverse.
- Capstone documentation: the catalog conjecture "8 reserved = signed
  singletons = self-dual bivectors" is FORMALLY VALIDATED at the
  Codeword type level.

**Out of scope:**

- Full M-10A implementation (Codeword ↔ Vector 5 ambient migration,
  IsReserved bridge as KernelCode form). This slice does the Reserved
  subset only; the full Codeword ambient migration is M-10A.
- The semantic correspondence (does the Reserved bit-pattern (b₀, b₁,
  b₂) match the SelfDual coefficient (c₀, c₁, c₂) semantically?).
  The slice establishes a bijection; whether it's THE "right" bijection
  in the catalog's mind is a separate question (the answer requires
  reading the catalog's intent for which Reserved bit maps to which
  Hodge-self-dual pair).
- Λ¹/Λ³ Hodge in F₂⁴ (the dim-4 self-dual half is at grade 2; grades
  1 and 3 have their own (trivial in F₂) Hodge ★).

## Step 2 — Search for repeatable form

The bridge exploits a shared 3-parameter F₂³ structure:

| Side | 3 free parameters | F₂³ identification |
|------|-------------------|---------------------|
| Codeword.Reserved | (bit₀, bit₁, bit₂) free Bool bits | via Bool ↔ F₂ componentwise |
| Vector 3 (M-11.dim4.reserved-bridge) | (c₀, c₁, c₂) free F₂ coefficients | identity |
| SelfDual ⊂ Λ²(F₂⁴) | sum of c₀ ·sd-pair-01-23 + c₁ · sd-pair-02-13 + c₂ · sd-pair-03-12 | via vector3-to-selfdual |

The composition Reserved ↔ Vector 3 ↔ SelfDual is the catalog
conjecture validation.

Per orbit-saturation: don't extract a generic "Bool³ ↔ F₂³" wrapper.
The instances are concrete (3 specific bits).

## Step 3 — Name the costructure (shadows)

**N-1 — `Bool↔F₂`** (in a new file `Substrate/Algebra/F2/FromBool.agda`):
  direct bijection `bool→F₂` and `F₂→bool` + both round-trips. ~30 LoC.

**N-2 — `reserved-to-vector3`**: extract the 3 free bits from a
  Reserved codeword + apply Bool→F₂ componentwise. ~10 LoC.

**N-3 — `vector3-to-reserved`**: inject a Vector 3 into a Reserved
  codeword (apply F₂→bool componentwise, set bit₃ = bit₄ = false,
  provide the IsReserved proof). ~10 LoC.

**N-4 — Round-trips**: backward-then-forward closes by Bool↔F₂
  round-trip applied 3 times; forward-then-backward similarly but
  requires Σ-type equality (for Reserved = Σ Codeword IsReserved).
  ~40 LoC.

**N-5 — `reserved-to-selfdual` + inverse**: compose N-2 with
  `vector3-to-selfdual` (from M-11.dim4.reserved-bridge); similarly
  N-3 with `selfdual-coefficients`. ~10 LoC.

**N-6 — Catalog conjecture capstone**: documentation block stating
  the validation: 8 reserved codewords ≅ 8 self-dual bivectors via
  the chirality-rotation framework. Cross-references the conjecture
  in catalog/cocycles.md. ~30 LoC.

## Step 4 — Composition

```text
Codeword.agda (Reserved : Σ Codeword IsReserved)
  ↓
N-1 (Bool ↔ F₂) ── componentwise ──┐
                                   │
                                   ↓
N-2 (reserved-to-vector3)  ⟷  N-3 (vector3-to-reserved)
                  ↓               ↓
                N-4 (round-trips)
                  ↓
M-11.dim4.reserved-bridge (Vector 3 ↔ SelfDual)
  ↓
N-5 (reserved-to-selfdual + inverse)
  ↓
N-6 (catalog conjecture capstone)
```

Composition: function composition + Bool↔F₂ componentwise application.

## Step 5 — Entailment

**Entailment claim:** the 8-element set `Reserved` (Codeword.agda's
8 reserved codewords) is in F₂-linear-isomorphic bijection with the
8-element set `SelfDual` (M-11.dim4's 8 self-dual bivectors). The
bijection respects the F₂³ structure on both sides.

This **formally validates the catalog conjecture** "8 reserved =
signed singletons" at the Codeword type level — the chirality-rotation
framework (Hodge ★ at grade 2 in F₂⁴) gives the structural
identification.

**BHK content:** N-5's `reserved-to-selfdual` IS the proof transformer:
given a reserved codeword, produces a self-dual bivector witnessing
the structural identification. The inverse closes the loop.

## Step 6 — Multi-angle reserve

If Σ-type equality for Reserved round-trip is awkward (the IsReserved
proof component requires lifting through equality):

- **Angle B — proof-irrelevance approach**: work at the proj₁ level
  (extract just the Codeword, then re-pack with the IsReserved proof).
  Σ-≡-intro becomes `cong (_, refl-component)`.

- **Angle C — pointwise equality via uniqueness**: prove the round-trip
  by ≡-from-lookup-style component analysis on Codeword (Bool-tuple
  componentwise).

If Bool ↔ F₂ bridge requires more than ~30 LoC (e.g., due to needing
the F₂-Like universal property), defer to M-10A and use a hand-rolled
local bijection here.

## Step 7 — Implementation order

1. **N-1** first (Bool ↔ F₂ in new FromBool.agda) — foundational.
2. **N-2** + **N-3** in a new bridge file (e.g.,
   `Substrate/Cocycles/V4Signature/Codeword/Reserved-To-Bivector.agda`).
3. **N-4** round-trips.
4. **N-5** composition with M-11.dim4.reserved-bridge.
5. **N-6** capstone documentation.
6. *Stop and commit* when the formal Codeword.Reserved ↔ SelfDual
   bridge closes cleanly.

## Effort estimate

| Shadow | Lines (est.) | Risk |
|--------|-------------|------|
| N-1 Bool ↔ F₂ | ~30 | low |
| N-2 reserved-to-vector3 | ~15 | low |
| N-3 vector3-to-reserved | ~15 | low |
| N-4 round-trips | ~50 | medium (Σ equality) |
| N-5 composition | ~20 | low |
| N-6 capstone | ~30 | low |
| **Total** | **~160** | **low-medium** |

## Success criteria

1. All files compile under `--safe --without-K`.
2. The bridge functions both type-check with correct signatures.
3. At least one round-trip closes (the simpler Vector 3 → Reserved → Vector 3).
4. The capstone reserved-to-selfdual + inverse functions are defined,
   making the formal Codeword.Reserved ↔ SelfDual bijection accessible.

## Lattice classification

- **Planning fire**: region #2 (DBE alone).
- **Implementation slice**: expected region #6 (DBE+S2G, sideways grid
  projection) — copies the Reserved-Bridge pattern from M-11.dim4 to
  a new pair (Codeword.Reserved ↔ Vector 3). No wrapper extraction.

## Connection to the full chirality-rotation framework

This slice CLOSES the formal connection between the chirality-rotation
framework (just shipped) and the actual Codeword type in the substrate.
After this:

- The catalog conjecture is validated end-to-end (Reserved ↔ SelfDual
  via the framework).
- Future M-10A (full Codeword ambient migration) can use this
  Reserved bridge as a starting point.
- Higher Hodge structure (grade 3 in F₂⁴, larger Hamming/RM) can build
  on the established pattern.

This is the LAST piece needed to claim the chirality-rotation framework
is GROUNDED in the substrate's actual data structures, not just an
abstract mathematical exercise.
