# GENERATED by scripts/gen_build_makefiles.py — do not edit by hand.
# Per-file, memory-confined Agda check for this directory.
# make = dispatcher only: it ALWAYS invokes agda per file; agda
# decides cached-vs-recheck (transitively correct). FILES and
# SUBDIRS are listed explicitly — a tree/makefile mismatch fails
# loudly (re-run scripts/gen_build_makefiles.py).

# membudget agda-shim → make is the controlled+instrumented build path (RAM-budgeted + proof-cost
# profiled). Idempotent (findstring); exported to recipes + sub-makes. MEMBUDGET_NOPROFILE=1 to skip
# profiling; `make AGDA=…` to pin a different agda.
SHIM_DIR := /home/mikemol/github/substrate/scripts/agda-shim
ifeq (,$(findstring $(SHIM_DIR),$(PATH)))
export PATH := $(SHIM_DIR):$(PATH)
endif

AGDA ?= agda
AGDA_FLAGS ?= --safe --without-K
MEM_CAP ?= 1024m
AGDA_ROOT := ..

FILES := Axes.agda Cardinality.agda Cocycle.agda Discipline.agda Foundation.agda Generators.agda ShadowArchitecture.agda
SUBDIRS := Algebra Axes Cardinality Category Cocycles Codes Conway FUSep Foundation Foundations Geometry Groups Invented Kelen Lambda Linguistic Logic Lojban Pedagogy Pipeline Probability Realizability S5 ShadowArchitecture Solresol TokiPona WitnessTower

.PHONY: all files subdirs $(SUBDIRS) check-sync clean

all: check-sync subdirs files

# Blow up if the directory's actual contents differ from this
# generated listing (new/removed file or subdir → regenerate).
check-sync:
	@disk_files=`ls *.agda 2>/dev/null | sort`; \
	 listed=`printf '%s\n' $(FILES) | sed '/^$$/d' | sort`; \
	 if [ "$$disk_files" != "$$listed" ]; then \
	   echo 'ERROR: $(CURDIR): .agda files differ from generated FILES list.'; \
	   echo '  on disk : '"`echo $$disk_files | tr '\n' ' '`"; \
	   echo '  listed  : '"`echo $$listed | tr '\n' ' '`"; \
	   echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	 fi

# One agda process PER FILE, RTS heap-capped, invoked from agda/
# so the .agda-lib / _build resolve. Always runs; agda no-ops if
# the interface is already current.
files:
	@for f in $(FILES); do \
	  echo "  check $(CURDIR)/$$f"; \
	  $(AGDA) $(AGDA_FLAGS) +RTS -M$(MEM_CAP) -RTS -i $(AGDA_ROOT) $(CURDIR)/$$f || exit 1; \
	done

subdirs: $(SUBDIRS)

Algebra:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Axes:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Cardinality:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Category:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Cocycles:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Codes:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Conway:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

FUSep:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Foundation:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Foundations:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Geometry:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Groups:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Invented:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Kelen:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Lambda:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Linguistic:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Logic:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Lojban:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Pedagogy:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Pipeline:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Probability:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Realizability:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

S5:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

ShadowArchitecture:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

Solresol:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

TokiPona:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

WitnessTower:
	@if [ ! -f $@/Makefile ]; then \
	  echo 'ERROR: $(CURDIR)/$@ has no generated Makefile (new directory?).'; \
	  echo '  Run scripts/gen_build_makefiles.py.'; exit 1; \
	fi
	@$(MAKE) -C $@

clean:
	@$(MAKE) -C Algebra clean 2>/dev/null || true
	@$(MAKE) -C Axes clean 2>/dev/null || true
	@$(MAKE) -C Cardinality clean 2>/dev/null || true
	@$(MAKE) -C Category clean 2>/dev/null || true
	@$(MAKE) -C Cocycles clean 2>/dev/null || true
	@$(MAKE) -C Codes clean 2>/dev/null || true
	@$(MAKE) -C Conway clean 2>/dev/null || true
	@$(MAKE) -C FUSep clean 2>/dev/null || true
	@$(MAKE) -C Foundation clean 2>/dev/null || true
	@$(MAKE) -C Foundations clean 2>/dev/null || true
	@$(MAKE) -C Geometry clean 2>/dev/null || true
	@$(MAKE) -C Groups clean 2>/dev/null || true
	@$(MAKE) -C Invented clean 2>/dev/null || true
	@$(MAKE) -C Kelen clean 2>/dev/null || true
	@$(MAKE) -C Lambda clean 2>/dev/null || true
	@$(MAKE) -C Linguistic clean 2>/dev/null || true
	@$(MAKE) -C Logic clean 2>/dev/null || true
	@$(MAKE) -C Lojban clean 2>/dev/null || true
	@$(MAKE) -C Pedagogy clean 2>/dev/null || true
	@$(MAKE) -C Pipeline clean 2>/dev/null || true
	@$(MAKE) -C Probability clean 2>/dev/null || true
	@$(MAKE) -C Realizability clean 2>/dev/null || true
	@$(MAKE) -C S5 clean 2>/dev/null || true
	@$(MAKE) -C ShadowArchitecture clean 2>/dev/null || true
	@$(MAKE) -C Solresol clean 2>/dev/null || true
	@$(MAKE) -C TokiPona clean 2>/dev/null || true
	@$(MAKE) -C WitnessTower clean 2>/dev/null || true
	@true
