# brick ledger: recovered/system-pi-v2.37.1/05-analysis-treatise.md (287 lines)
# states: DONE/DONE; append insights as ACT:/OBL:/GLU:/COC: lines under each brick
H1 1-144 DONE
H2 145-287 DONE
Q1 1-72 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
M 73-215 DONE-FRESH 2026-06-11 (re-executed in two prints; diff below)
Q4 216-287 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
A 1-215 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
B 73-287 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
A2 1-215 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
B2 73-287 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)

## H1 capture (lines 1-144) — typed
- ACT: treatise created 16 MINUTES after the four parts — the self-analysis
  pass over the just-sealed artifact. HoTT + Earley + simplicial complexes;
  "Meaning is Structure" as operational constraint.
- ACT exec summary: the documentation is a FIBRATION over the executing
  logic — "a text that proves itself as it is read." GLU(B2/v3.8):
  proof-carrying documentation stated as homotopy lifting (3.3: every
  logical path lifts to an explanatory path; every explanation projects to
  a verification path; "the text IS the territory, projected into a higher
  dimension").
- ERRATUM(source): "frozen into a mutable RootHash" — context (frozen,
  entropy-resistant, part-4 Immutable Foundations) makes "immutable"
  unambiguous; one-word slip, typed as erratum not cocycle. Also
  "orthoganol" sic.
- ACT 1.2: SIMPLICIAL COMPLEXES as the knowledge structure; graphs
  (1-complexes) insufficient for multi-way relations; k-simplices model
  A-B-C as a unified FACE. GLU: the atlas's joint dep-projections are
  faces; the brick complex; Simplicial Complex Representation Learning
  cited (GNN literature — see 8.1 in H2).
- ACT 2.1: Earley chosen for COMPLETENESS over speed; any CFG; ambiguity
  -> parse forest; un-parsable input is "ontologically non-existent" —
  V-kind as ontology.
- ACT 2.2 PARSING AS DEDUCTION named with its literature family
  (computational linguistics / logic programming — the deductive-parsing
  lineage Goodman's semiring parsing extends). GLU(B7): the source
  anchors externally exactly where the review faulted the atlas. Axioms =
  terminals; inference rules = productions; goal = start symbol; six-step
  derivation: Axiom of Terminal Truth at leaves + structural induction
  propagates to root => IsTrue(S). "A crash is impossible... Grammar
  Failure... rejected from the topos before it can execute."
- ACT 2.3 BootstrapSequence: MIME grammar loads FIRST — "the medium
  itself is subject to parsing"; KernelProver_Self_Parse_Verified =
  Grammar Reflexivity ("the watchman watches himself first").
- GLU(proc1 pre-registration CASHED): 3.1 LOJBAN/AMR SEAL — Lojban gives
  one-to-one syntax (function from morphology + position; no garden
  paths); AMR gives semantics as rooted DAG (:arg0/:arg1),
  machine-traversable. A TWO-WITNESS validity boundary. GLU
  (chip-metaprompt): the English/AMR/Lojban mutual-witness trio is the
  user's own skill — the seal is cogrounded methodology.
- TENSION-CANDIDATE COC-3 (LEM in the L-table): 3.2 Completeness/Gap row
  — "a Gap implies a proposition neither true nor false, VIOLATING THE
  LAW OF EXCLUDED MIDDLE within the topos" — against the source's own
  constructivism (part 2 removed AoC), intuitionistic air-gapped contexts
  (part 3), and the Governor's BOTTOM-? value. RESOLUTION SKETCH
  (stratified logic): 4.3 says "no undecidable propositions IN THE
  TERMINAL LAYER" — LEM scoped to the terminal bedrock of a declared
  Closed World; U/BOTTOM-? live at non-terminal strata. Pending H2/Q4
  for confirmation.
- OBL DISCHARGED (part-2 Negative Resolution): the L-table says the
  system "must effectively model the Closed World" — closed-world is an
  EXPLICIT, owned modeling commitment at the terminal/decision stratum;
  the sec.-3 tension resolves as declared pinning. The treatise states
  it; the obligation closes.
- ACT 3.2 table: Conceptual Shortcut ("...") = un-reified deferral,
  REJECTED (Zero Elision's dual); FOUNDATIONALISM is a FLAW ("Because I
  said so" rejected; authority from structural consistency) —
  anti-foundationalism: cogrounding as doctrine; Flaw deviation triggers
  immediate RE-PARSING ("the document's self-defense mechanism").
- ACT 4.2 FINITENESS OF REALITY: an infinite non-terminal is a
  "Potential," not a "Fact." GLU: part-2 RealDef (Reals = infinite
  refinement = Potentials); S19 oscillating witnesses are Potentials —
  the source's Fact/Potential split is the grace theorem's stratification.
- ACT 4.3: Atomic Isolation purchases PARALLEL PROVING (disjoint
  branches concurrently — sheaf locality as parallelism); Halting
  Guarantee O(n^3); Structural Emergence (meaning = parent non-terminal).
- ACT 4.4: grammar as ERROR-CORRECTING CODE ("a flipped bit is not an
  alternative truth; it is a grammar failure"); Cryptographic Uniqueness
  bridges Terminal-as-Truth to the RootHash.
- ACT 5.1 HoTT-OS: types = spaces, terms = points, EQUALITY = PATHS
  ("two processes are equal... if a continuous path transforms one into
  the other"). GLU(STRONG, S25): witnesses-are-paths is the source's
  kernel semantics — the atlas's witness strata are HoTT path spaces at
  the instrument level.
- SEAM: H1 ends mid-5.1; 5.2 Path Induction opens H2; M (73-215) covers.

## H2 capture (lines 145-287) — typed
- ACT 5.2 PATH INDUCTION as security: secure at reflexivity + all ops are
  invariant-preserving paths => secure in every reachable state; "if no
  path exists, the transition is topologically impossible." GLU: charter
  reachability gate; part-3 actuator impossibility — security by
  path-nonexistence.
- ACT 5.3 HOMOLOGICAL STABILITY named with the mathematics (H_i stabilizes
  as n grows): as modules load, the system's homology stabilizes.
  GLU(NEW THEORY THREAD): the atlas HAS this phenomenon empirically —
  circles/clusters persisted as the space grew 13,824 -> 165,888 across
  v3.3-v3.8; truth-separators identical across v3.5/v3.5a. Circle
  persistence under knob extension = homological stability of the claim
  complex. Candidate spec framing; queue it.
- ACT 5.3/6.1: a Topos "supports constructive logic"; Closed World
  Assumption NAMED ("nothing exists outside the parse tree") — the
  part-2 OBL discharge confirmed twice; "no debt in the economy of truth."
- COC-3 RESOLVED BY STRATIFICATION (and earned, not assumed): 3.2 demands
  LEM in the topos; 5.3 says topoi are constructive. Resolution: the
  TERMINAL layer is decidable (4.3 halting guarantee, O(n^3)) —
  decidability PURCHASES excluded middle locally; the bedrock is Boolean
  because decidable, the superstructure constructive. GLU: the atlas's
  U-kinds live exactly where decidability fails (noisy/partial locks) —
  same stratification: LEM where decidable, U where not. Downgraded from
  tension-candidate to resolved-with-mechanism.
- ACT 6.2: the Simplicial Resolution "mirrors computing the BETTI
  NUMBERS"; "a coherent system has a specific Betti profile (likely
  trivial in the dimensions that correspond to contradiction)."
  GLU(chip-metaprompt): H0/H1/H2 sketches compute exactly this; our
  per-document Cech verdicts are Betti-profile statements.
- ACT 6.3: axiom independence as linear independence; Kolmogorov-
  complexity commitment (max structural signal / min lexical noise).
- COC-2 CONFIRMED (the principal finding): 7.2 re-asserts Empty Queue,
  explicitly forbidding Coq-style "admit" in the final artifact, and
  cites the Gap Correction — while RationalDef dangles in ALL FIVE
  documents (treatise scan: zero hits; only "operational"/"radical"
  substring matches). The self-audit AND the 16-minutes-later analysis
  pass both missed a live "admit." The source lacks an UNAUDITED
  honesty class; the atlas's v3.8 UNAUDITED row is the repair pattern.
- COC-1 CONFIRMED: 7.3 quotes the nominal string AS the RootHash and
  explains Merkle verification over it — the analysis pass treats the
  name as the value; content-addressable existence claimed, address
  never computed. Externally discharged by the recovery snapshot's
  CHECKSUMS.sha3-512.
- ACT 7.4: Immutable Infrastructure — no patching, no learning; change =
  new artifact + new RootHash; "a static, crystalline structure of
  logic — a Terminal Simplicial Complex."
- ACT 8.1: GNN -> simplicial parallel; "logical holes (non-trivial Betti
  numbers) that a linear compiler would miss." GLU: 2nd-order breakers
  ARE logical holes; the witness-stratum machinery detects non-trivial
  classes.
- ACT 8.2: Saussure/Lacan — the Symbolic (Grammar) structures the Real
  (Terminals); Lojban/AMR = "Perfect Language" aligning signifier and
  signified. 8.3: attacks as topologically disconnected paths; Table 2
  traditional-vs-Pi comparison. IX: "when meaning is structure, truth
  is inevitable."
- ACT WORKS CITED (22 refs): includes PARSING AS DEDUCTION (ACL
  P83-1021, Pereira-Warren 1983 — canonical), the HoTT book,
  homological stability (Wahl ICM), Merkle/integrity, Lojban, AMR
  survey, simplicial representation learning — and #6 "EFFICIENT
  SEMIRING-WEIGHTED EARLEY PARSING": the semiring bridge between source
  and atlas sits in the source's OWN bibliography. GLU(B7, STRONG): the
  citation-backbone task becomes merge-and-extend of the source's
  works-cited with the review's required set; the overlap (deductive
  parsing, semiring parsing, HoTT) is itself cogrounding evidence.

## Gluing passes (Q1, M, Q4, A, B, A2, B2)
- SEAM(H1|H2 at 144/145): cut mid-5.1; M (73-215) holds 5.1-7.2 whole.
  PASS. Q1 (exec summary + I-II) and Q4 (7.3-IX + works cited)
  consistent on re-read; A/B/A2/B2: no further overlap conflicts.
- DOCUMENT CECH VERDICT: intra-treatise clean except where the treatise
  inherits the artifact's claims (COC-1, COC-2 re-asserted, not caught).
  COC-3 resolved within the treatise itself (decidability-stratified
  LEM). ERRATUM: "mutable RootHash" (exec summary) for "immutable."

## ERRATUM (see cotype S36): the H2-capture line "the overlap is itself
## cogrounding evidence" is WITHDRAWN — the review/literature-map was a
## user-relayed message about the ATLAS TARBALL, not system corpus;
## bibliographic overlap between two LLM-assembled lists is common-cause,
## not convergence. The works-cited entries remain candidate anchors for
## B7 under verify-then-import. Original text retained per append-only.

## ERRATUM (author correction, 2026-06-11) — works-cited provenance
The H2 ACT entry attributing the 22-entry works-cited to the source is
WITHDRAWN. The list is the external reviewer's output (provided to the
author after it reviewed the atlas script) and is not January corpus.
Body-inline references survive as the source-side anchors (see
recovered/system-pi-v2.37.1/PROVENANCE-NOTE.md). The "merge-and-extend
+ overlap as cogrounding" line is corrected in cotype S36.

## REGRADE (S37, fiber-certificate honesty applied to the reading)
Brick verification classes for this document: H1, H2 = PERFORMED READS
(fresh window prints). Q1/M/Q4/A/B/A2/B2 = ASSERTED-FROM-HELD-CONTEXT
(gluing claims over text in working context; no fresh window pass).
Coverage executed: ~1x with one seam, not the designed ~4.5x. DONE
status retained for capture bookkeeping; the PASS verdicts on non-H
bricks carry this weaker certificate class. Exception note: proc1's M
was examined as a seam unit and yielded a finding (1.1.1.gamma
arrival), evidence the M-layer pays when performed.

## REGRADE (author correction): the Q/M/A/B brick entries above were
## gluing ASSERTIONS over context held in a lossy window, not fresh
## window-passes. In-context recall is not a certificate. M-layer
## (seam windows) now being re-executed as fresh prints with capture
## diffs; Q and A/B layers honestly DEFERRED pending decision.

## M fresh-pass capture-diff (lines 73-215, seam 144|145) — 2026-06-11
- SEAM NOW PERFORMED (was asserted): the 5.1 paradigm-shift paragraph +
  5.2 + 5.3 read whole across the cut. PASS, earned.
- NEW — COC-2 SHARPENED TO AN OPERATIONAL PREDICTION: Table 1 (3.2)
  states that any Flaw-deviation "triggers an immediate re-parsing...
  and the boot process will HALT," and a Gap = missing parse path. The
  dangling RationalDef IS a Gap under the source's own definition;
  therefore the source's self-defense mechanism predicts the offered
  First Operational Bootstrap HALTS at RealDef -> RationalDef
  resolution. COC-2 upgrades from claim-tension to a falsifiable
  runtime prediction about the artifact.
- NEW (structural reuse note): treatise 5.3 files Resistance-to-
  Collapse and Cycle-Preservation under Phase-1 Topos Stability;
  part 4 presents the same two bullets under Phase-4 Coherence —
  verbatim reuse across phase boundaries; not a conflict, but the
  treatise re-files Coherence content as Stability content.
- ERRATUM(minor): "This axioms enforce" (4.2), sic.
- VERDICT: fresh pass yielded the COC-2 operational sharpening — the
  largest single yield of the M-layer. Four for four.
