# brick ledger: recovered/system-pi-v2.37.1/03-part3-proc4.md (132 lines)
# states: PENDING/DONE; append insights as ACT:/OBL:/GLU:/COC: lines under each brick
H1 1-66 DONE
H2 67-132 PENDING
Q1 1-33 PENDING
M 34-99 PENDING
Q4 100-132 PENDING
A 1-99 PENDING
B 34-132 PENDING
A2 1-99 PENDING
B2 34-132 PENDING

## H1 capture (lines 1-66) — typed
- GLU(part2 turn prompt): "Stability" ARRIVED as pre-registered.
- ACT I: stability = GRAMMATICAL property — stable iff the evaluator proves
  all trajectories resolve to a terminal equilibrium sequence.
- ACT 1.1 Temporal Convergence (bounded epsilon-neighborhood of attractor);
  1.2 Structural Resistance = parent non-terminal invariant under small input
  perturbations ("Robust Parsing"). GLU(B11): the source defines stability
  exactly as the review defined COGROUNDING strength — invariance under
  perturbation; 1.3 Deterministic Flow (no leakage into un-parsed space).
- ACT 1.1.1 LYAPUNOV GRAMMAR: Lyapunov functions as measure-grammars with
  strictly non-increasing terminal outputs. GLU(STRONG, S19): the grace
  theorem's refinement schedule IS a Lyapunov grammar (monotone q_n =
  monotone error bound); lock-schedule monotone axis adjacent.
- ACT 1.1.2 Quiescent Resolution (HALT/STEADY terminal ends refinement).
  NOT a tension with S19 oscillation: part 2 typed Reals as INFINITE
  refinement (non-terminal) — the source stratifies terminating systems
  from non-terminating refinements itself. GLU(part2 RealDef).
- ACT 1.1.3 BIBO: finite input -> finite-depth parse tree.
- ACT 1.2.1 Error-Correcting Homology: production rules DISTANT in
  grammar-space (bit-flips cannot jump non-terminals) — code-distance
  reading of grammar design. GLU: separator count as distance.
- ACT 1.2.2 BIFURCATION THRESHOLDS: explicit terminal limits beyond which
  the grammar MUST FORK to keep descriptive power. GLU(STRONG): the knob-
  admission rule — KNOB_PROVENANCE instruments exactly this phenomenon
  (correction event -> space forks). The source names our governance move.
- ACT 1.2.3 MARGIN OF TRUTH: distance from current parse to nearest
  negated-stability terminal. GLU: S24 breaker-proximate cells (8,825) =
  a margin measurement; separator counts as margins.
- ACT 1.3.1 PHASE-SPACE COVERING: grammar covers every valid terminal
  sequence to avoid "Unknown". GLU(charter): the coverable gate, in source.
- ACT 1.3.2 Ergodicity of Parsing: all valid states visited given time —
  the exhaustive sweep is this guarantee purchased by finiteness.
- ACT 1.3.3 Entropy-Aware Transitions: information loss in observation must
  be ACCOUNTED. GLU: Remark 3.6 encode-don't-lose (loss only by declaration).
- ACT 1.1.1.alpha monotonic descent checked at every clock-terminal;
  1.1.1.beta GLOBAL ATTRACTOR: all parse-paths converge to one target
  terminal — verdict-level confluence; the atlas refines this with
  witness-level path classes (2nd-order strata distinguish what the
  attractor identifies); 1.1.1.gamma resource conservation (rule
  applications decrease in t — cost-Lyapunov).
- ACT Phase-3 MIME: ControlSystemDef (Plant EBNF(Input,State)->State;
  Controller EBNF(Reference,State)->Input; stability proof: L(S_{t+1}) <
  L(S_t), controller "prunes divergent parse-paths"). DynamicalSystemDef
  (Flow EBNF(State,Time)->State; Invariance: M invariant iff productions
  from M land M-parsable). GLU: invariant set = the source's circle —
  atlas circles are invariant sets under knob flow.
- SEAM: H1 ends AT the section-III header ("Ethical Pruning") — body
  entirely in H2; boundary covered by M (34-99).
