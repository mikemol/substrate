# brick ledger: recovered/system-pi-v2.37.1/03-part3-proc4.md (132 lines)
# states: DONE/DONE; append insights as ACT:/OBL:/GLU:/COC: lines under each brick
H1 1-66 DONE
H2 67-132 DONE
Q1 1-33 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
M 34-99 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
Q4 100-132 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
A 1-99 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
B 34-132 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
A2 1-99 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)
B2 34-132 ASSERTED-FROM-HELD-CONTEXT (regrade 2026-06-11: not a fresh pass)

## H1 capture (lines 1-66) — typed
- GLU(part2 turn prompt): "Stability" ARRIVED as pre-registered.
- ACT I: stability = GRAMMATICAL property — stable iff the evaluator proves
  all trajectories resolve to a terminal equilibrium sequence.
- ACT 1.1 Temporal Convergence (bounded epsilon-neighborhood of attractor);
  1.2 Structural Resistance = parent non-terminal invariant under small input
  perturbations ("Robust Parsing"). GLU(B11): the source defines stability
  exactly as the review defined COGROUNDING strength — invariance under
  perturbation; 1.3 Deterministic Flow (no leakage into un-parsed space).
- ACT 1.1.1 LYAPUNOV GRAMMAR: Lyapunov functions as measure-grammars with
  strictly non-increasing terminal outputs. GLU(STRONG, S19): the grace
  theorem's refinement schedule IS a Lyapunov grammar (monotone q_n =
  monotone error bound); lock-schedule monotone axis adjacent.
- ACT 1.1.2 Quiescent Resolution (HALT/STEADY terminal ends refinement).
  NOT a tension with S19 oscillation: part 2 typed Reals as INFINITE
  refinement (non-terminal) — the source stratifies terminating systems
  from non-terminating refinements itself. GLU(part2 RealDef).
- ACT 1.1.3 BIBO: finite input -> finite-depth parse tree.
- ACT 1.2.1 Error-Correcting Homology: production rules DISTANT in
  grammar-space (bit-flips cannot jump non-terminals) — code-distance
  reading of grammar design. GLU: separator count as distance.
- ACT 1.2.2 BIFURCATION THRESHOLDS: explicit terminal limits beyond which
  the grammar MUST FORK to keep descriptive power. GLU(STRONG): the knob-
  admission rule — KNOB_PROVENANCE instruments exactly this phenomenon
  (correction event -> space forks). The source names our governance move.
- ACT 1.2.3 MARGIN OF TRUTH: distance from current parse to nearest
  negated-stability terminal. GLU: S24 breaker-proximate cells (8,825) =
  a margin measurement; separator counts as margins.
- ACT 1.3.1 PHASE-SPACE COVERING: grammar covers every valid terminal
  sequence to avoid "Unknown". GLU(charter): the coverable gate, in source.
- ACT 1.3.2 Ergodicity of Parsing: all valid states visited given time —
  the exhaustive sweep is this guarantee purchased by finiteness.
- ACT 1.3.3 Entropy-Aware Transitions: information loss in observation must
  be ACCOUNTED. GLU: Remark 3.6 encode-don't-lose (loss only by declaration).
- ACT 1.1.1.alpha monotonic descent checked at every clock-terminal;
  1.1.1.beta GLOBAL ATTRACTOR: all parse-paths converge to one target
  terminal — verdict-level confluence; the atlas refines this with
  witness-level path classes (2nd-order strata distinguish what the
  attractor identifies); 1.1.1.gamma resource conservation (rule
  applications decrease in t — cost-Lyapunov).
- ACT Phase-3 MIME: ControlSystemDef (Plant EBNF(Input,State)->State;
  Controller EBNF(Reference,State)->Input; stability proof: L(S_{t+1}) <
  L(S_t), controller "prunes divergent parse-paths"). DynamicalSystemDef
  (Flow EBNF(State,Time)->State; Invariance: M invariant iff productions
  from M land M-parsable). GLU: invariant set = the source's circle —
  atlas circles are invariant sets under knob flow.
- SEAM: H1 ends AT the section-III header ("Ethical Pruning") — body
  entirely in H2; boundary covered by M (34-99).

## H2 capture (lines 67-132) — typed
- GLU(part2 turn prompt): "Ethical Pruning" ARRIVED as pre-registered.
- ACT III: Governor = SYNTACTIC FILTER, not moralist; prunes intent
  pre-reification.
- ACT 2.1 SYNTACTIC PROHIBITION: Harm-terminal actions are UN-PARSABLE —
  "do not exist as valid expressions." GLU(STRONG): prohibition implemented
  as DE-STATING — forbidden != false, forbidden = unstatable. The atlas's
  V-vs-F distinction deployed NORMATIVELY; the sec.-3 prohibition pattern
  (prohibit by removing from the language) is the source's ethics.
- ACT 2.2 Sieve-Filtering on the SPPF (named in source — the B11 carrier:
  "build the SPPF and get the metatheory for free" is the source's own
  substrate). 2.3 Self-Consistency: ethical iff execution preserves the
  system's own QuiescenceProof.
- ACT 2.1.3 Forbidden Bit-strings: Actuators ARCHITECTURALLY unable to
  process Negated terminals — constructible impossibility (charter:
  not-coverable BY CONSTRUCTION, enforcement below policy).
- ACT 2.2.3 Heuristic Safety: under "Unknown," default to MOST RESTRICTIVE
  sub-grammar — a declared decision POLICY attached to the U-verdict.
- ACT 2.3.1 MORAL HOMOLOGY: avoid "cycles of harm" — an action that
  eventually negates its own justification is a cycle in the epistemic
  graph. H1-flavored ethics; cascade-cocycle adjacency.
- ACT 2.3.3 Axiomatic Loyalty: Governor is the ONLY component able to veto
  a MorphismGenerator violating AxiomAsHomologyCycle. OBL(NEW TERM):
  AxiomAsHomologyCycle — definition expected part 4/treatise; PRE-REGISTER.
- ACT 2.1.1.alpha: pruning rule must be REACHABLE from every valid
  non-terminal (the charter's reachability gate applied to the prohibition
  itself); .beta: pruning must not prune the KernelProver (no
  self-decapitation); .gamma SYMMETRIC VERIFICATION: A pruned in C =>
  every A' with the SAME TERMINAL HOMOLOGY pruned in all C' — enforcement
  factors through the homology quotient (well-defined on classes; the
  witness-stratum reading of fair enforcement).
- ACT Phase-3.5 MIME: EthicalGovernorDef — NegationGrammar
  EBNF(Harm_Terminals); FilterRule SPPF => Pruned_SPPF;
  Governor_Soundness: Watchdog on leaf-nodes; a matching leaf makes the
  parent return the value BOTTOM-? and the action stays "Unknown" and
  UN-EXECUTABLE. GLU(NVL, STRONG): a non-classical verdict value at the
  enforcement point, in source — Belnap-flavored carrier; prohibition
  resolves to paraconsistent Unknown, NOT to F.
- ACT WorldModelFunctorDef: ExternalObservationCategory ->
  InternalGrammarCategory; Integrity = internal terminals are a valid
  parse of external observations. GLU: OB-3 AB-functor target; Remark 3.6
  at the world boundary.
- ACT Audit: "Stability" upgraded conceptual -> concrete Lyapunov-Grammar
  "ensuring the mathematical claim is parsable" — DERIVE-OR-RENAME IN
  SOURCE (B6's pattern is the source's own audit move); SPPF expanded
  everywhere (anti-elision); Governor tied to QuiescenceProof so it
  cannot "suicide by pruning its own foundational logic."
- OBL(turn prompt): Part 4 = Phase 4 Self-Reification Engine + Phase 5
  Quiescence, "final closure and the cryptographic integrity seal" —
  PRE-REGISTER: the seal (proc1 Lojban pamoi-liste pointer should cash
  out), QuiescenceProof definition, AxiomAsHomologyCycle definition.
  RationalDef STILL DANGLING after part 3 — carried forward.

## Gluing passes (Q1, M, Q4, A, B, A2, B2)
- SEAM(H1|H2 at 66/67): cut at the section-III header; body in H2; M
  (34-99) holds header + body across the cut. PASS.
- Q1/Q4/A/B/A2/B2: no overlap conflicts; "terminal", "SPPF",
  "TopologicalTruthEvaluator", "QuiescenceProof" used consistently.
- INTER-COMPLEX UPDATE: the part-2 tension-candidate (Negative Resolution
  vs sec. 3) WEAKENS — part 3 shows the source keeping Unknown
  un-executable via BOTTOM-? (paraconsistent value, no collapse to F) and
  defaulting restrictive under U: decision-stratum pinning CONFIRMED as
  the source's consistent pattern. OBL to treatise remains for explicit
  statement, downgraded from tension-candidate to
  resolution-sketch-supported.
- CECH VERDICT: 0 intra-document cocycles, H0-only.

## REGRADE (S37, fiber-certificate honesty applied to the reading)
Brick verification classes for this document: H1, H2 = PERFORMED READS
(fresh window prints). Q1/M/Q4/A/B/A2/B2 = ASSERTED-FROM-HELD-CONTEXT
(gluing claims over text in working context; no fresh window pass).
Coverage executed: ~1x with one seam, not the designed ~4.5x. DONE
status retained for capture bookkeeping; the PASS verdicts on non-H
bricks carry this weaker certificate class. Exception note: proc1's M
was examined as a seam unit and yielded a finding (1.1.1.gamma
arrival), evidence the M-layer pays when performed.

## REGRADE (author correction): the Q/M/A/B brick entries above were
## gluing ASSERTIONS over context held in a lossy window, not fresh
## window-passes. In-context recall is not a certificate. M-layer
## (seam windows) now being re-executed as fresh prints with capture
## diffs; Q and A/B layers honestly DEFERRED pending decision.
