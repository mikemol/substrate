power-config experiment: arms = ['performance', 'balanced', 'power-saver']; AC online = None
  (finer arms root-gated: EPP x5, governor x2, no_turbo x2, per-core caps -- not swept)

  performance  clock 4.39 GHz | compute  156.6 GFLOP/s | mem  14.7 GB/s | ridge  10.7 FLOP/byte 
  balanced     clock 2.20 GHz | compute   92.1 GFLOP/s | mem  10.5 GB/s | ridge   8.8 FLOP/byte 
  power-saver  clock 1.45 GHz | compute   60.3 GFLOP/s | mem  10.2 GB/s | ridge   5.9 FLOP/byte 

  restored: power-saver

  1. ABSOLUTE scales with arm: performance clock 4.39GHz/compute 157 vs power-saver 1.45GHz/60 (lever real) True
  2. RIDGE invariant across arms: [10.7, 8.8, 5.9] FLOP/byte (spread 45%) -- clock cancels in the ratio False

  PARTIAL — the power arms are an experiment factor: absolute
  throughput is the arm-dependent lever (measured ~2.6x compute,
  ~3.0x clock), while the RIDGE ratio stays ~invariant -- the model's
  clock-cancels claim, now ground-truthed across the real power configs. Finer arms (EPP,
  per-core caps) are root-gated next-granularity factors.
