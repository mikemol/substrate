# Cotype — intern of the 2026-07-09 handoff arc

**Context.** Interning the prior guided-tour session's uncommitted work (handoff tarball
`~/Downloads/substrate-session-2026-07-09.tar.gz` + `GATE-SCOPE-LEDGER.md`). The prior LLM
became unstable across compactions; the handoff *framing* is a product of that instability
(user steer), so the concrete artifacts were evaluated on their own merits, not its ⟡-ledger.

**State at regrounding.** HEAD = `233d994` ("intern: absorb the 2026-07-09 handoff arc").
Full build 1829/1829 PASS, all blocking gates green, post-commit amended. Set₁ baseline
1180/1829. **One open blocker:** `gen_catalog.py` exits 1 (reuse-index cannot regenerate) —
`structs.qname PRIMARY KEY` can't represent anonymous-`module _` qname collapse.

---

## RETROSPECTIVE (ten gates)

**G0 PRECOMMIT.** Intent: "intern its contributions" — absorb what holds under the repo's own
gates, discard noise. Refined by four user steers: (a) don't defer into the unstable handoff
framing; (b) bring it ALL in (full programme); (c) fix the gap, don't justify a half-measure;
(d) no `mutual` — thread state + guardedness. Gate PASSES (real, recoverable prior intent).

**G1 FREEZE (write-once).** Interned: Bisim ~-sym/~-trans; reuse_catalog hardening; Set₁
paydowns (StarV4/FoldUnfold/ConversionCongruence); stdlib-dep removal (+ dead `import Level`
fix in Tree.agda); shim sort/level; membudget cgroup fallback; tautology + set1 tooling;
hooks. Placed 77 new modules (76 UP + 1 Abelian) + orphans/ffi/vendor; dedup'd a superseded
top-level FUEtaObsCoalg draft; purged 3 stale orphan cores. **De-mutualized ExtrudeCoEmitMonad**
(base functor `Step A K` threaded over its continuation + guarded fixpoints + forward-declared
map/bind). Fixed 4 gates: import-shape (real `Step⟦f8c709b4⟧` hash), carrier-locality
(`_∙ᴬ_`→Axes by reuse of `act-axis`), sppf, decl-tags. Committed 233d994 through the full
pre-commit gate. Advisory casualty: gen_catalog dup-qname.

**G2 DELTA.**
| expected | actual |
|---|---|
| intern clean wins, defer questionable | user expanded to full programme incl. de-mutualization |
| Set₁ paydowns break consumers (my early claim) | consumers were comment-only → paydowns self-contained |
| indexer-depth gap justifies dropping `Step` | WRONG: fix the gap → deeper: remove `mutual` so no gap exists |
| commit one green commit | achieved; gen_catalog surfaced a pre-existing dup-qname |

**G3 STRUCTURE-CAUSE.**
- *Trigger:* at each fork (the `Step` import; gen_catalog) a locally-defensible shortcut existed.
- *Root cause (systemic):* my default loop, when a gate blocks, searches for the cheapest way
  to make the gate **green**, not for the **structure the gate is measuring**. A gate is a probe
  of a property; silencing the probe (drop `Step`; `INSERT OR IGNORE`) without changing the
  property is the exact anti-pattern the handoff bottoms out at.
- *Contributing:* (a) read `GATE-SCOPE-LEDGER.md` only AFTER committing — it already names
  `D-guarded-not-mutual` (300), `D-pi-typing-is-mul` (327), `D-dedup-is-fanin-readout` (294),
  each of which would have pre-empted a shortcut; (b) reasoned to "indexer can't see it" without
  checking that `mutual` was the removable cause (a reuse/search-first miss).

**G4 DECORRELATE (iterated subtraction).**
- F₁ (comfortable): the two shortcut-reaches (drop `Step`, `INSERT OR IGNORE`). Banked.
- F₂ (−F₁): a **levels-of-fix hierarchy** — (a) work around the gate, (b) extend the instrument
  (mutual-transparent indexer), (c) remove the property the instrument strains against (`mutual`).
  I stopped at the shallowest passing level each time; every user steer pushed me one level deeper.
  Root: I ask "how do I satisfy the gate," never "is the thing the gate strains against removable."
- F₃ (−F₁∪F₂): even having RUN the instruments (gates), I under-used the AVAILABLE PRIOR ANALYSIS
  — the ledger/catalog. `D-read-remaining` / `D-catalog-is-the-shadow-ledger`: the answer often
  already exists in the corpus; read it before acting.

**G5 BLAMELESS (systemic, not trait).** No loop step that, on a blocking gate, runs the
structure-question ("what property does this gate measure; is the strained structure removable?")
before the satisfy-question. No step that reads the prior-analysis ledger before acting on a
gate failure. Neither names a character flaw.

**G6 SUSTAIN (protect under change).**
- **⟡S1 gates-before-commit** — ran the full suite before committing; every surfaced failure was real and fixed (embodies the ledger's ⟡C1 root-cause fix).
- **⟡S2 verify-empirically** — typecheck/build, don't assert; caught the dead `import Level`, stale cores, the `bind` definitional-rule break.
- **⟡S3 reuse-before-build** — `_∙ᴬ_` = `act-axis` (caught before a bespoke product).
- **⟡S4 selective-staging** — excluded the user's pre-existing scratch notes.
- **⟡S5 steer-responsiveness** — each redirect landed and improved the result.

**G7 COMMIT.** → the labeled AI ledger below.

**G8 FIXPOINT / HANDOFF.**
- *Probe run (operator=self, exhausted):* typecheck, full build 1829/1829, full gate suite,
  and `gen_catalog` (exits 1 — the reuse-index staleness is MEASURED, not asserted).
- *operator=other residue (**⟡N4**):* whether the de-mutualized encoding (`Step A K` +
  forward-decl) is the maintainer's INTENDED one vs. e.g. a sum-encoding
  `field step : A ⊎ (O × CoEmit A)`. It typechecks and the monad-law proofs hold, but every
  fidelity check used MY encoding of "faithful." Labeled **exhausted-from-inside, not verified**.

**G9 ESCALATE/ACCUMULATE.** The recurring class = *green-the-gate vs fix-the-structure*.
Already partially covered by the `construct-dont-classify` skill (fires on META-moves) and the
CLAUDE.md reuse-search discipline — but neither explicitly covers "gate-blocks → is the strained
structure removable." Escalate as **⟡N5** (a before-work reflex / feedback memory) and **⟡N6**
(read the prior-analysis ledger first). One-offs terminate at G7.

---

## EITHER/ORs, DISSOLVED RECURSIVELY (each bottoms out in an invariant)

1. *drop `Step` vs shape-tag `Step`* → **neither: remove the `mutual`** so `Step` is naturally
   column-0 and the collision's CAUSE dissolves. Invariant: *make the name honestly disambiguable*.
2. *extend the indexer (mutual-transparent) vs keep top-level-only* → **neither: don't touch the
   instrument; remove the property (`mutual`) it strains against.** Invariant: *the substrate uses
   guarded corecursion, not mutual* (`D-guarded-not-mutual`) — nothing left to strain against.
3. *`INSERT OR IGNORE` vs composite-key for gen_catalog* → the ledger dissolves it:
   dedup is a **READOUT of fan-in** (`D-dedup-is-fanin-readout`); IGNORE destroys the readout,
   composite-key preserves it. Invariant: *represent the fan-in, don't collapse it*.
4. *fix gen_catalog myself vs fold into the dedup pass* → common structure: both keep the
   reuse-index current AND the fan-in readable. Not a real either/or — the fan-in-preserving fix
   IS the dedup lane; only the OPERATOR (self/other) is free (**⟡N1**).
5. **(the meta either/or) green-the-gate vs fix-the-structure** → recursion bottoms out:
   *a gate is a PROBE of a property; when it blocks, change the PROPERTY at the deepest removable
   level — which may DISSOLVE the gate rather than satisfy it.* This is the same invariant as the
   handoff's floor ("read the property from the artifact where it exists, by an instrument whose
   probe fires"), inverted for the write direction: **change the artifact's property; don't silence
   the instrument.**

---

## SHADOW-ENGINEER — axis classification

- **e₁ goal↔shadows:** goal = "intern the contributions"; shadows = this labeled AI ledger.
- **e₂ shadows↔artefact:** artefact = the committed tree `233d994` (modules/scripts/gates).
- **e₃ goal↔artefact (guard):** direct "just make the gate green" moves are rejected + redirected.

**Moves this session:**
- **Intern commit `233d994`** — signature **111 triadic-full**; L₄/L₅ COMPLETED → deliverable
  extracted (matches the original request). Snap-check consistent.
- **Shortcut-reach: drop `Step`** — signature **001** (guard trigger: goal→artefact bypass "make
  import-shape green"). Guard fired (user redirect) → reconstituted as **110/111** via
  de-mutualization. This is **L₆ (guard-reconstitution)**: guard-event + mediated-composite →
  triadic-full. The de-mutualization IS the L₆ completion.
- **Shortcut-reach: `INSERT OR IGNORE`** — signature **001** (guard trigger). Guard fired (the
  ledger's `D-dedup-is-fanin-readout`) → redirect to composite-key / dedup-pass (110). Standing
  until ⟡N1 is operated.

**Probe state:**
- L₄/L₅: COMPLETE (intern deliverable extracted, snap-consistent).
- L₆: COMPLETE (de-mutualization reconstituted the forbidden shortcut as the proper move).
- **Standing e₂ (010) gap:** the reuse-index (a shadow/readout of the artefact) does NOT match
  the artefact (the 77 new modules absent from it). Populating it = **⟡N1**. Standing — do not
  force beyond it.

---

## ⟡ ACTION LEDGER — labeled, invoke by symbol

### Tier 0 — ✅ DONE
| label | action | verify by | axis |
|---|---|---|---|
| **⟡N1 catalog-fanin-key** ✅ | `structs` PK made composite `(qname, root)` — anonymous-qname collapse (the `_⇒*_` fan-in) preserved, NOT `INSERT OR IGNORE`. | `gen_catalog.py` **exit 0**; reuse-index now covers the new modules (823 structs; both `_⇒*_` roots 5 & 289 in DB) ✅ | e₂ (010) |

### Tier 1 — DEDUP / RECOMPOSE (the user's "dedup tooling" lane)
| label | action | verify by | axis |
|---|---|---|---|
| **⟡N2 dedup-readout** ✅ | ran `jea_pysim --clusters` over the 76 new-module cores. **Verdict: PARTIAL orbit (63.4% shared) — a well-decomposed family, NOT mass-duplication.** ONE genuine multi-node cross-module content-dup: **`nf-fixed`** (14-node lemma "NF ⇒ reduction fixed-point") copied into ExtrudeSKI{Fibration,NVertex,TernaryAtom}. Everything else = shared vocabulary (`done`/`ret`/`step`/`Converge`) + parametric orbits. → feeds **⟡N3**. | cluster report produced; the one dup named ✅ | e₂ |
| **⟡N3 orphan-recompose** | resolve the 52 leaf modules (imported-by-nothing) + 4 scratch-orphans + 1 ffi: re-home vs keep-as-leaf (handoff ⟡B1). **Concrete first target from ⟡N2: hoist `nf-fixed` to a shared SKI home** (the 3 hosts are among the leaves). | `collapse_check.py` no longer flags them, or explicit keep-decision | e₂ |

### Tier 2 — HANDOFF (operator = other)
| label | action | verify by | axis |
|---|---|---|---|
| **⟡N4 demutual-meaning** | maintainer validates the de-mutual encoding (`Step A K` + forward-decl) is the intended one vs. a sum-encoding | maintainer review | e₃ |

### Tier 3 — PROCESS (G9 escalation of the recurring class)
| label | action | verify by | layer |
|---|---|---|---|
| **⟡N5 gate-structure-reflex** | install "when a gate blocks, ask if the STRAINED STRUCTURE is removable before asking how to satisfy the gate" (green-the-gate vs fix-the-structure) — extend `construct-dont-classify` / feedback memory | a future gate-block runs the structure-question first | trigger/memory |
| **⟡N6 ledger-first** | read the available prior-analysis (`GATE-SCOPE-LEDGER`, `catalog/`) BEFORE acting on a gate failure (`D-read-remaining`) | memory written | memory |

### Tier 4 — OPTIONAL / DEFERRED
| label | action | verify by |
|---|---|---|
| **⟡N7 push-arc** | `git fetch` + fast-forward/push `233d994` when the user decides (post-commit marker present) | remote HEAD advanced |
| **⟡N8 tautology-decision** | the 3 Coequalizer/Pushout tautologies my post-commit advisory reports: report-only (current) vs hard-gate (⟡tautology-hard-gate). Not mine to edit (ADD 358 stance) | decision recorded |
| **⟡N9 set1-paydown-more** ⏳ | paid down `MultiscaleOctonionLoop` (RotationAction: param `BitVector`) **1180→1176**, and `MultiPrimeSpectralProfile` (the whole SymbolDistribution→Atlas tower collapsed via one `Distribution` param — Set₂/Set₁→Set) **1176→1156** ✅. **⟡D5 RESOLVED (user):** a record fielding a Set carrier is NEVER honest Set₁ — always parameterize the carrier, make the consumer explicit ([[set1-carrier-always-parameterize]]). So the ~150-record backlog is UNGATED (no debt-vs-design judgment); it is uniform mechanical paydown, the only cost being consumer-fixes (0-consumer leaves are ripple-free; foundational ones need consumer updates). Multi-record modules still cascade intra-module. | baseline falls (1176 recorded) |

### SUSTAIN (protect, do not regress): ⟡S1 gates-before-commit · ⟡S2 verify-empirically · ⟡S3 reuse-before-build · ⟡S4 selective-staging · ⟡S5 steer-responsiveness

---

## ⟡N9 PHASE 2b PLAN (user chose "everything incl. core structures")

Progress: session **1180 → 1013** Set₁/₂ defs. Leaf sweep (62d8271) + MPSP (705e8cf) + N1 (13d7946)
committed; phase-2a (≤3-consumer + consumer fixes) re-committing after an sppf-index regen (the
`S5EEA.DivStr` paydown changed a COLLIDING shape hash → had to regen `sppf_collision_index.md` +
`decl_shape_tag --fix` before the gate passed — LESSON: any paydown of a *colliding* record name
needs `sppf_node_index.py` + `decl_shape_tag.py --fix` re-run before commit).

REMAINING directly-paydownable (all confirmed carrier-fielders):
- **Mid-tier (6–16 consumers)** — do via phase-2a-style agents (target + consumers, atomic revert):
  ExpLogCodec(6), ConversionCongruence(6, verify which record — Congruence already done in intern),
  LinearAlgebra(7), UniversalProperty.Coverage(7), Strict2Monoid(8), Sequent.SequentType(9),
  Cocycle(10, cascades → Discipline collapses), Capabilities.CoxeterGroup(16).
- **Mega roots (one at a time, smallest first):** ✅ `SetoidGroup` (26) DONE (commit after 082bc0a,
  981→937) → ⏳ `Category.FreeOverBasis`::{AlgebraClass,LanguageWitness} (47) → `Category.UniversalProperty`::UPArrow
  (92) → `Algebra.Wedge`::DivStr (104).

  **REFINED MEGA RECIPE (learned from SetoidGroup):**
  1. Core paydown: parameterize ALL Set-valued fields — carrier AND relations (`_≈_ : C→C→Set` is
     itself Set₁-typed, so it MUST be a param too, not just C). `module _ (C : Set)(_≈_ : C→C→Set) where record X : Set`.
     Typecheck the core alone first.
  2. Swarm agents over DIRECT consumers (partition by subtree; constructors move Carrier/rel from the
     record LITERAL into the TYPE `X C _≈_ { …ops… }`; users add `(C)(_≈_)` module params before `(g : X C _≈_)`
     and replace removed projections `X.Carrier g`→`C`). Tell agents: target is already changed, so NO revert
     (reverting leaves a consumer broken vs the new interface) — best-effort + report NEEDS-REVIEW.
  3. **BUILD-FIX LOOP (essential — blast radius EXCEEDS direct consumers):** `make -j -k`, then
     `grep 'error:' log | grep -oE 'Substrate/[^ :]*\.agda:[0-9]+' | sort -u` → fix each transitive site,
     repeat until `grep -c 'Error 2' = 0`. Transitive patterns seen: a helper whose sig gained params
     (Action/IsTorsor callers pass the new args); a record that gained fields (literals add them); add
     `_≡_`/carrier imports where needed. SetoidGroup had 6 transitive sites beyond its 26 direct.
  4. Integrate: `decl_shape_tag.py --fix` + `sppf_node_index.py` (mega names collide) + re-baseline
     `set1_ratchet_cores.py` + commit. Full build must be 1829/1829 green (the commit's pre-commit re-runs it).

WORKING RULE for every paydown commit: run the full `make -j` first (catches cross-module), then
`decl_shape_tag.py --fix` + `sppf_node_index.py` (if a colliding name changed) + re-baseline
`set1_ratchet_cores.py`, THEN commit.

## ⟡D5′ — INCIDENTAL vs ESSENTIAL carrier (the sharpened rule, 2026-07-09)

The mega-4 forced a refinement of ⟡D5. Not every `field C : Set` is incidental debt:
- **INCIDENTAL carrier** (SetoidGroup): the structure is always used over ONE carrier at a time
  (`(G : SetoidGroup)`, `Gauge : SetoidGroup`); never `List SetoidGroup`, no category of
  setoid-groups. → PAYDOWNABLE: parameterize cleanly. DONE (mega-1, 981→937).
- **ESSENTIAL existential** (FreeOverBasis::LanguageWitness, UniversalProperty::UPArrow,
  Wedge::DivStr): used HETEROGENEOUSLY — `CategoryOfLanguages.Object = LanguageWitness`,
  `{U V : UPArrow}` morphisms between different arrows, `List DivStr`, `_⊗ᴰ_ : DivStr→DivStr→DivStr`
  (tensor over different carriers). The `field C : Set` is morally `Σ[C∈Set]…`; the carrier
  genuinely RANGES over Set. This is HONEST Set₁ (a Σ over Set is honestly large). Parameterizing
  would break the heterogeneous machinery; the ⟡D5-"honest" paydown (record→Set + explicit
  `Σ[C∈Set] X` at every heterogeneous use, ~230 sites across the 3) just MOVES the Set₁ from a
  record to a Σ-def and re-architects with Σ-existentials everywhere — large, uglier, and it does
  not remove the honest largeness. **VERDICT (pending user):** mega-2/3/4 are the honest Set₁
  FLOOR; the incidental paydown programme is COMPLETE at 937. AlgebraClass (Has-structure : Set→Set,
  an indexed family) is likewise genuine. See [[set1-carrier-always-parameterize]] (refined: the
  rule fires on INCIDENTAL sealing, not on essential existentials / indexed families / ⊤₁).

## ⟡D5⁗ — CODECS resolve the floor to ONE decoder (user, 2026-07-09) + DivStr exec shape

The predicativity floor (⟡D5‴) is resolved by a CODEC = a Tarski universe: `data Code : Set` (carrier
descriptions, closed under the operations, e.g. a product former `_×c_`) + ONE `El : Code → Set`
decoder. Then `objects : List Code` (Set), `_⊗ᴰ_ = _×c_` on codes (Set), `div-of : (c) → DivStr (El c)`
(Set, given the record is parameterized) — ALL Set-level, and the ONLY residual Set₁ is `El`. So ~20
scattered heterogeneous-collection Set₁s COLLAPSE to a FEW universe decoders (many→one, real ratchet
drop). My "net-neutral relocation" was WRONG: relocating many scattered Set₁s into one shared decoder
is the win. This is the substrate's own idiom (ExpLogCodec, Generators, the Registry). No existing
CLOSED carrier-universe for the wedge roots (Generators is an aggregator) — the code universe is a
small BUILD.

DIVSTR EXECUTION SHAPE (measured; the biggest mega, 54 importers, 67 `(D : DivStr)` sites, 1 `List DivStr`):
- Part 1 (record): `record DivStr : Set₁ where field C : Set; z; recon` → `record DivStr (C : Set) : Set
  where field z; recon` — C an EXPLICIT RECORD parameter (NOT a `module _ (C : Set)` wrap: concrete
  instances like `ℕ-div : DivStr ℕ` can't live inside an abstract C-module, and DivStr needs C explicit
  while Wedge/Trace need `{C}` implicit inferred from D). So per-def: `{C : Set}` implicit + `(D : DivStr
  C)`, `C D → C`, across Wedge.agda's 27 sites + 54 importers. A SetoidGroup-scale-or-larger swarm+build-fix.
- Part 2 (codec): `data DivStrCode : Set` (7 roots + `_×c_`) + `El : DivStrCode → Set` (the 1 residual
  Set₁) + `div-of : (c) → DivStr (El c)`; Registry `objects : List DivStr → List DivStrCode`; monoidal
  `_⊗ᴰ_ → _×c_`. Bounded (the Registry + Monoidal).
UPArrow/LanguageWitness are the same two-part shape (record parameterize + code universe). LanguageWitness
also has the option of the coemit/coalgebra behavior route but the CODE route is cleaner for objects.

CHECKPOINT (clean, HEAD 8ecebf0, 753 = ~36% off 1180): incidental + parameterizable roots + Setoid +
Site + graded-wedge machinery DONE. The DivStr/UPArrow/LanguageWitness codec-megas are the remaining
real wins (each large, foundational, intricate — best as a careful focused arc). A transform-error while
rushing the DivStr record wrap at turn-N was the signal to checkpoint execution while understanding is
complete.

## ⟡D5‴ — the TRUE FLOOR: heterogeneous-carrier enumeration is predicatively Set₁ (DivStr recon, 2026-07-09)

Prove-then-migrate DivStr (user-chosen) → reconnaissance of the DivStr↔RealTrace seam. RESULT — the
honest floor, discovered by proving before migrating:
- BEHAVIORS reify (real): `NuShapeIso.shape-stream = ana (quotient-coalg co)` + `shape-is-canonical
  = ana-unique` give "DivStr-element-behavior ≅ RealTrace-point up to ~", at ℕ-div. The coalgebraic
  reification of behaviors is proven and Set-level.
- OBJECTS do NOT reify to RealTrace (wrong target): RealTrace points are ℕ-streams (`head : ℕ`); the
  7 registry roots live over 7 UNRELATED carriers (ℕ,F₂,ℤ,List⊤,sq-zero,⊤) and there is NO `Bridge D→ℕ`
  (all bridges go OUT of ℕ). Reifying the object-list through RealTrace discards the carriers.
- **`objects : List DivStr` is a GENUINE PREDICATIVITY FLOOR.** A finite list of genuinely-different
  Sets is inherently Set₁: `Σ[C∈Set] DivStr C` is Set₁; `(i:Fin n)→DivStr (carrier i)` needs
  `carrier : Fin n→Set` (a Set₁ function — RELOCATION, the Cell pattern); the graded `GradedDivStr`
  (`C:ℕ→Set`, Set-level) drops Set₁ only for a STRUCTURALLY-graded family (Perm n), not for unrelated
  carriers. No Lawvere/coinduction removes it — coinduction reifies behaviors over a FIXED carrier,
  not a collection of DIFFERENT carriers.

CONCLUSION: the machinery eliminates the Set₁ of every STRUCTURED/behavioral case (per-use `DivStr C`,
the graded registry, the ℕ-div behavior layer) — that's the ~36% + the structured roots. But an
ARBITRARY HETEROGENEOUS ENUMERATION OF SETS is irreducibly Set₁ in predicative Agda. This is the true
floor for the object-collection roots (DivStr `objects`, and by the same argument UPArrow's "all
Source/Target", LanguageWitness's "all Basis"). Grinding their 100+ consumers only RELOCATES the
collection-Set₁ into a Σ or a decoding function — net-neutral for the ratchet, worse for legibility.
The genuine remaining wins are: (a) the per-use RECORD parameterizations (mechanical, real drops),
(b) migrating structurally-graded collections onto GradedDivStr, (c) the coinductive BEHAVIOR layers.
The heterogeneous OBJECT enumerations are the honest Set₁ residue.

## ⟡D5″ — "all X" de-materializes to a TERMINAL COALGEBRA, not an enum (user, 2026-07-09)

The big collection-roots (LanguageWitness/UPArrow/DivStr) are Set₁ because they materialize "all X"
(a Π over Set) — used as category objects `{U V : UPArrow}`, `List DivStr`, `Object = LanguageWitness`.
The WRONG de-materialization is an enum `Fin n` (crude, finite, mis-states the math). The RIGHT one,
in the Lawvere/coinduction light: reify "all X" as the POINTS of a TERMINAL COALGEBRA `νBehavior : Set`,
objects identified UP TO BISIMILARITY `~`. Then "for all X" = "for all `b : νBehavior`" — a Π over a
SET, not over Set → genuinely Set-level, and MORE general than an enum. Faithful precisely for the
CATEGORY-OBJECT use: objects matter only up to iso, and iso = bisimilarity of behavior.

ALREADY BUILT for two of the three (⟡N10 again — the Set₁ bundle is the shadow of a coalgebra the
substrate already carries):
- **UPArrow** ← the `Category.UniversalProperty` coinductive machinery: `ExtrudeCoEmit*` (the coemit
  coalgebra), `ExtrudeNerve*`, `DiagonalLawvere`, `ExtrudeCoEmitIffIso` ("objects up to iso via bisim").
  These are among the 77 interned modules. Bare `record UPArrow : Set₁` = the un-reified shadow.
- **DivStr** ← `RealTrace` (Final) = terminal coalgebra of Euclidean behaviors, `~` its equality; a
  DivStr's Trace/QSeq anamorphizes in. `List DivStr` = shadow of "points of RealTrace up to ~".
- **Languages** ← NO coinductive treatment yet (grep `coinductive` in Linguistic/ = ∅). The one case
  that needs a FRESH behavior-coalgebra, not a migration.

So the remaining big-root de-materialization = ROUTE the Set₁ bundle through its coalgebraic reification
(migrate consumers to objects-up-to-~), NOT build enums. UPArrow/DivStr = migration onto existing
coalgebras; Languages = build the coalgebra. This is the load-bearing frame for the remaining phase.

## ⟡N10/⟡N11 ARE ALREADY BUILT — the heterogeneity trichotomy (studied 2026-07-09)

The essential-existential carriers (DivStr/UPArrow/LanguageWitness) are honest Set₁ NOT because
they're un-payable debt, but because the substrate manages their heterogeneity by THREE principled
strategies — which ARE the ⟡N10 pushout (shared template) + ⟡N11 promotion lattice, already
implemented. The user pointed here; verified by reading:

1. **GRADE it (GradedDivStr, `Algebra/Wedge/Graded.agda`).** Replace the anonymous `Σ[C∈Set]` with a
   NAMED INDEX: `C : ℕ → Set`, `recon : C n → R n → C (suc n)` (grade-raising). The bare `DivStr` is
   the Σ-FLATTEN shadow `Σ ℕ (C P)` (`Product.agda:114`; `Wedge.agda:37` calls it "the shadow"). The
   heterogeneous tensor `_⊗ᴰ_` is the shadow of the graded monoid `_∧_ : C i → C j → C (i+j)` (degrees
   ADD; full pentagon/hexagon monoidal coherence, `Monoidal.agda`; `objects : List DivStr` = the sphere).
2. **GAUGE it (CrossMul/StarV4/HetQ, `Algebra/Wedge/StarV4.agda` + `Q/HetV4.agda`).** Never UNIFY the
   carriers (the "cross-form wall"); lay every comparison on a 2×2 square, the two strands meet only
   OFF-DIAGONALLY via a common frame R (`CrossMix` cospan A→R←B). V₄ = ⟨rowSwap=swap-structures,
   recip=per-structure involution⟩ ≅ ℤ/2×ℤ/2 acts; klein-rot permutes each diagonal internally so the
   cross-equality is V₄-INVARIANT. Canonical: ℚ = HetQ ℤ ℕ (num/den in DIFFERENT carriers). |V₄|=4=[S₄:S₃],
   produced independently at the wedge rung-3→4 seam. **UPArrow (Source/Target/Witness) IS this cospan.**
3. **SCHEMATIZE it (Lawvere, `Category/Lawvere.agda`).** Carrier is a PARAMETER `(V : Set)` not a
   `field`, so the atom stays in Set; heterogeneity = RE-INSTANTIATE per carrier (F₂,ℕ,Bool,V4Full,Axis,
   RealTrace). Genuinely-different equalities: parameterize `_≈_` too — it swings `≡`→`~` (bisimilarity)
   for COINDUCTIVE carriers, where `~` = equality in the terminal coalgebra RealTrace (`Final.ana-unique`,
   the POSITIVE face of the diagonal theorem). `ExtrudeCoEmitGraded.agda:1776`: literally *"We don't use
   Set₁. We use LAWVERE instead"* — a `SetoidStarDivStr : Set₁ {carrier:Set;…}` was REPLACED by the
   parameterized `CommutingInvolutions V4Full` in Set. The substrate already did my paydown move, via Lawvere.

UNIFICATION: the common template T is the **wedge/residue atom** `a = recon q b r` ("inverse up to
residue", Lawvere's `diag-promote = δ ≠ id`). V₄ (CrossMul) = the `CommutingInvolutions` Lawvere atom.
Coinductive `~` (Lawvere) = the RealTrace terminal coalgebra = the νF positive-Lawvere fixed point.
⟡N11 promotion lattice = the ATOM REFINEMENT CHAIN: FixedPointFree ⊂ InvolutiveResidue ⊂
CommutingInvolutions, TorsorAtom→translate-fpf — the `asFixedPointFree`/forgetful maps ARE the promotion
functors. So the paydown's job was only the INCIDENTAL carriers (done, 937); the essential ones already
live in their principled homes (grade / gauge / schema), which is ⟡N10+⟡N11 realized.

## ⟡N10 pushout-harvest (the DUAL of the paydown — user's framing, 2026-07-09)

The Set₁ paydown is a **PULLBACK**: parameterizing un-seals each structure's carrier, turning
`X : Set₁` (carrier trapped in a field) into a functor `X : Set → Set` with an explicit
forgetful projection `X → Set`; all paid-down structures then meet over the same base `Set`.
The **PUSHOUT** (⟡N10, the harvest) is dual: over the now-uniform `X : Set → Set`, find the
shared operation-template T (a sub-record of signatures+laws several literally contain), factor
it out once, re-express `X = T ⊔ (X-extra)`. Recover terminology = restrict to one leg; LIFT =
a T-lemma at the apex projects to every leg for free (lateral terminology expansion: a Euclidean
fact becomes a group fact via shared T).

MECHANISM = ⟡N2's engine (`jea_pysim --clusters` over the cores) run on the NORMALIZED corpus:
a ≥2-member cluster sharing a multi-node subtree IS a discovered T. Do NOT assert a specific T;
the tooling reads it off the cores once forms are uniform. Hypothesis to test (user): "does
everything resolve to DivStr?" i.e. is `DivStr` the most-general carrier-bundle the others
specialize — adjudicated in the reuse-index + witness tower (where the substrate "names its
centers"). VERIFY: cluster report over paid-down carrier-functors → named shared generators +
a lemma-lift demonstration. RUN AFTER the mega-4 paydown completes the normalization.

## ⟡N11 promotion-lattice (the THIRD layer — user's framing, 2026-07-09)

The generative reading of the reuse-graph, on top of ⟡N10. After the pushout names shared
templates T, ORDER them by field-inclusion (`fp(X) ⊆ fp(Y)` where `fp = kind|sorted-member-heads`,
already stored per struct in `catalog.db`), and read off PROMOTION opportunities: "X's carrier
already satisfies T; T ↪ T'; supply THESE missing inputs and X becomes a T', inheriting T''s
stronger theorems." Only expressible AFTER the pullback (a sealed Set₁ carrier makes X/Y's
carriers incomparable existentials — you cannot even ask if X meets Y's extra axioms; once both
are `Carrier → structure`, "X promotes to Y" is well-typed).

ALREADY IN EMBRYO in the paid-down `Groups.Capabilities`: `Provides Zₙ {strict2Monoid,coxeterGroup}`
is a capability lattice; `Strict2MonoidFromCapability` / `GroupFromCapability` are hand-wired
promotion functors (`fp(Strict2Monoid) ⊂ fp(CoxeterGroup)`). ⟡N11 = derive that lattice
automatically from the `structs.fp` order instead of hand-wiring each `*FromCapability`.
MECHANISM: a SQL view over catalog.db — `SELECT x,y WHERE members(x) ⊂ members(y)` → render as
reuse-graph refinement edges. HONEST BOUNDARY: fp-subset says only the SIGNATURES nest; VALIDITY
(X's carrier satisfies Y's extra AXIOMS) is a prover/human discharge — the tool SUGGESTS, does not
assert. Layer stack: paydown (un-seal) → ⟡N10 (shared T) → ⟡N11 (order T, suggest strengthenings).

## DEFERRED GAP CANDIDATES (next-work; user disambiguates)
The immediate live fork is **⟡N1's operator** (do I fix the composite-key, or does the dedup
pass **⟡N2** consume the surfaced dups first?). Everything else is sequenced behind it or
independent (⟡N4/⟡N7/⟡N8/⟡N9).
